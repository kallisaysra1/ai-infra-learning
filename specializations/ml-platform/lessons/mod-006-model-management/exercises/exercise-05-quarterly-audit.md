# Exercise 05: Quarterly Audit Tooling

Build `quarterly_audit.py` that:
- Lists every Production model
- Checks each for: current model card, bias review (if applicable), recent audit entries
- Produces a Markdown report grouped by team
- Surfaces models out of compliance

Run against a simulated registry with mixed compliance state.
