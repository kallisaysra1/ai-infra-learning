# Ex 03: PyTorch profiler + TensorBoard

Use `torch.profiler` on a transformer training step. Identify Python-side
overhead (DataLoader idle? optimizer hotspot?).

Deliverable: TensorBoard screenshot + 5-bullet diagnosis.
