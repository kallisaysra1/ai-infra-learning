# interactive_tuples.py

# Here is our model evaluation results (a Tuple is created with parentheses)
eval_results = (0.12, 0.95)  # (loss, accuracy)

# TASK 6: Unpack this tuple into two separate variables named "loss" and "accuracy", and print the accuracy.
loss, accuracy = eval_results
print(accuracy)
